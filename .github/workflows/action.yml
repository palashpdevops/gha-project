name: First Workflow
on: push
env: NAME="Palash"
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: echo "${NAME} Hello World!"
    - name: Print goodbye
      run: echo "Done - bye!"
